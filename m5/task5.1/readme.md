# Module 5 Linux Essentials. TASK 5.1
## Part 1
1) Log in to the system as root.
2) Used the passwd command to change the password.
3) Determined the users registered in the system, as well as what commands they
execute.
4) Changed personal information about current user.
5) Became familiar with the Linux help system and the man and info commands.
6) Explored the more and less commands using the help system. Viewed the contents
of files .bash* using commands.
7) * Described in plans work on laboratory work 1.
8) * Listed the contents of the home directory using the ls command, defined its files
and directories.
Task1.Part2
1) Examined the tree command. Listed subdirectories of the root directory up to
and including the second nesting level.
2) Used command that can be used to determine the type of file.
3) Mastered the skills of navigating the file system using relative and absolute paths.
4) Became familiar with the various options for the ls command.
5) Performed the following sequence of operations:
- created a subdirectory in the home directory;
- in this subdirectory created a file containing information about directories
located in the root directory (using I/O redirection operations);
- viewed the created file;
- copied the created file to your home directory using relative and absolute
addressing.
- deleted the previously created subdirectory with the file requesting removal;
- deleted the file copied to the home directory.
6) Performed the following sequence of operations:
- created a subdirectory test in the home directory;

- copied the .bash_history file to this directory while changing its name to
labwork2;
- created a hard and soft link to the labwork2 file in the test subdirectory;
- changed the data by opening a symbolic link. What changes will happen and
why
- renamed the hard link file to hard_lnk_labwork2;
- renamed the soft link file to symb_lnk_labwork2 file;
- deleted the labwork2.
7) Using the locate utility, found all files that contain the squid and traceroute
sequence.
8) Determined which partitions are mounted in the system, as well as the types of
these partitions.
9) Counted the number of lines containing a given sequence of characters in a given
file.
10) Using the find command, found all files in the /etc directory containing the
host character sequence.
11) Listed all objects in /etc that contain the ss character sequence.
12) Organized a screen-by-screen print of the contents of the /etc directory.
13) Determinde the type of device.
14) Determined the type of file in the system.
15) *Listed the first 5 directory files that were recently accessed in the /etc
directory.
![Image](https://github.com/Deodatuss/DevOps_online_Kyiv_2021Q2/blob/main/m4/task4.2/screenshots/PacketTracer_rkWepb4RUt.png)
